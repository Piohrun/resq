# resQ 1.8.0 candidate evidence

This directory commits the compact qualification record for candidate
`9ed7fdd2fb68f5dad354e81d33ff92522fc64e4e`:

- `release-audit.json` is the schema-v1 gate summary and exact candidate record.
- `checksums.sha256` authenticates every file in the complete raw evidence
  archive attached to the GitHub release.

The full archive contains command logs, normal and isolated reports,
differential, coverage, compatibility, scale, soak, installation, state, and
schema artifacts. Its SHA-256 is
`69292db999423dddd664979419f09af082611a0e09c16ce7018796cd935ee3d9`.

The final tag's attached audit is generated by re-running the same gate on the
certification commit itself; it is authoritative for the tag's exact SHA.
